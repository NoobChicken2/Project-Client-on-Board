{
  "scripts" : {
    "server": "npm --prefix ./server run dev",
    "client": "npm --prefix ./client run dev",
    "dummy": "npm --prefix ./converter-dummy-api run dev",
    "start": "npm run client && npm run server"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "bootstrap-icons": "^1.10.2",
    "express": "^4.18.2",
    "jwt-decode": "^3.1.2",
    "svelte": "^3.53.1"
  },
  "devDependencies": {
    "@types/pg": "^8.6.5"
  }
}
